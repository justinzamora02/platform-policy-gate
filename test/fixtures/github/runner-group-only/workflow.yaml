# GHA-003 fail fixture: without a runner-group allowlist, a group-only selector
# cannot be approved from its labels.
name: build
on:
  pull_request:

permissions: {}

jobs:
  build:
    runs-on:
      group: private-runners
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.0.0
